#!/usr/bin/env sh
# project-harness:managed version=0.1.0
set -eu
ROOT=$(CDPATH= cd -- "$(dirname -- "$0")/.." && pwd)
PYTHON=${PYTHON:-python3}
exec "$PYTHON" "$ROOT/dev/harness.py" "close-plan" "$@"
