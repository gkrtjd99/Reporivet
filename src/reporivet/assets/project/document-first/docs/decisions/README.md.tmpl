# Decisions

Use this directory for durable product, architecture, security, operations, or process decisions whose rationale and consequences must survive the current Plan.

- Use `_template.md` for a new ADR.
- Record the chosen boundary, rejected alternatives, consequences, verification, and retirement trigger.
- Mark replaced decisions superseded and link both directions; do not rewrite historical rationale.
