<!-- reporivet:entrypoints:start -->
# {{PROJECT_NAME}} agent entrypoints

{{PROJECT_SUMMARY}}

Reporivet provides optional installed-CLI discovery help; it does not install a repository runtime. After Reporivet is uninstalled, ordinary Markdown, manifest, test, and code discovery must remain possible.

## 수명 주기 안내

- 이 블록 밖의 사용자 프로젝트 지침은 직접 편집하고 보존합니다.
- 이 블록 안의 Reporivet 내용은 내부 재생성·덮어쓰기 대상입니다. 문서를 이동한 뒤 설치되어 있다면 `AGENTS.md`가 있는 root에서 `reporivet upgrade --root .`를 실행하고, 필요하면 `--claude`도 사용합니다.
- package가 없어도 이 일반 Markdown은 수동 편집할 수 있습니다.
- 이 안내는 명령을 실행할 자동 권한을 부여하지 않습니다.

## Observed entry paths (not authority)

The bounded list below is observation only. Confirm existing user rules and current source-of-truth documents before relying on it. Distinguish current evidence, history, and conflicts. Read only files needed for the task. Do not execute commands without permission. Report changes and verification exactly as observed.

{{OBSERVED_PATH_ROWS}}

Excluded from this bounded observation: {{OBSERVED_EXCLUSIONS}}

Semantic routing and ownership remain in this file's user-owned text and the repository's existing documents. Reporivet does not create a route database, document schema, Gate, plan runtime, task store, or hidden state.
<!-- reporivet:entrypoints:end -->
