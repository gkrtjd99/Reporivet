#!/usr/bin/env sh
# reporivet:managed version={{HARNESS_VERSION}}
set -eu
ROOT=$(CDPATH= cd -- "$(dirname -- "$0")/.." && pwd)
PYTHON=${PYTHON:-python3}
exec "$PYTHON" "$ROOT/dev/harness.py" "{{COMMAND}}" "$@"
