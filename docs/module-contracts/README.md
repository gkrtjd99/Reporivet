# Module Contracts

> Historical/superseded record. This directory preserves the retired module-contract surface for readability only. It is not current routing, a package input, or a setup output. Route through [`../README.md`](../README.md), current authority documents, and the active Markdown Plan.

The historical contract family described durable multi-file boundaries from explicit repository evidence. Do not create new module contracts from this record or treat one contract per directory as a current requirement. Current document-first setup uses canonical Markdown authority and ordinary Plans; it does not generate code maps or module contracts.

The historical body below records its former ownership and evidence rules. Retired command instructions have been removed; use [`../QUALITY.md`](../QUALITY.md) and [`../README.md`](../README.md) for current checks and routing. Existing records remain readable and are not current evidence.

Create a `MOD-*.md` contract only for a durable multi-file boundary whose scope is supported by an existing repository path, an explicit historical command configuration, or a confirmed planned path in an active product specification.

Do not create one contract per directory. A contract records only explicit ownership, responsibility, applicable paths, public entry points, dependency rules, organization, and verification commands. Use `unknown` when evidence is unavailable rather than guessing.

The former generator workflow is retired. Preserve existing module-contract records for history and use the current document-first authority and project-owned checks rather than invoking a retired repository command.
